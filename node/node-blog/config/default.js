const config = {
  // 启动端口
  port: 3000,
  // 数据库配置
  database:{
    DATABASE: 'nodesql',
    USERNAME: 'karen',
    PASSWORD: '993698262',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config